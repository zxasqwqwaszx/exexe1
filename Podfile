# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Wetswipe' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Wetswipe

  pod "Koloda"
  
  platform :ios, '11.2'
  pod 'SDWebImage', '~> 4.0'

  target 'WetswipeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'WetswipeUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
